const prompt = require('prompt-sync')()
const { saveFile, readFile } = require('./utils')

// console.log('oi')
// const texto = prompt('Digite o texto: ')
// console.log('Foi digitado', texto)

// saveFile('exemplo.txt', 'meu conteúdo')
// const conteudo = readFile('projetoLindo.md')
// console.log(conteudo)

/* Classe Cidade
  - Nome (string)
  - Ruas (array(Rua))
  - Bairros (array(string))
  - QtdHabitantes (number)
*/
/* Classe Rua
  - Nome (string)
  - PrimeiroNumero (number)
  - UltimoNumero (number)
  - CEP (string)
  - Feira (bool)
*/
