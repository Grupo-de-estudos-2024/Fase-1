// 1- Qual o último index do seguinte array?
// ['a', 'b', 'c', 'd']
// R: 3

// 2- Qual o último index do seguinte array?
// [1, 3, [1, 2, 3], 8]
// R: 3, pois o array interno conta como um único elemento

// 3- Qual propriedade pode ser utilizada para descobrir
// o tamanho de um array?
// R: length

// 4- Qual a diferença entre uma posição e um index?
// R: a posição e o index são forma de ordenação.
// Entretanto, a posição começa-se a contar em 1
// e o index começa a contar em 0

// 5- Como calcular o último index do array?
// R: assumindo que existe uma variável chamada 'array' que contém um array:
// array.length - 1

// 6- Como adicionar um elemento ao final do array?
// R: assumindo que existe uma variável chamada 'array' que contém um array
// e que existe uma variável chamada 'novoElemento' que contém um elemento:
// array.push(novoElemento)

// 7- Como adicionar um elemento ao começo do array?
// R: assumindo que existe uma variável chamada 'array' que contém um array
// array.unshift()

// 8- Como remover um elemento final do array?
// R: assumindo que existe uma variável chamada 'array' que contém um array
// array.pop()

// 9- Como remover um elemento inicial do array?
// R: assumindo que existe uma variável chamada 'array' que contém um array
// array.shift()

// 10- Como ordenar um array?
// R: assumindo que existe uma variável chamada 'array' que contém um array
// array.sort()

// 11- Como substituir um elemento do array?
// R: assumindo que existe uma variável chamada 'array' que contém um array
// e que queremos substituir um elemento no index 1:
// array[1] = 'novo elemento'
// Exemplo procurando o elemento 'Paçoca' e substituindo por 'Anninha':
// let array = ['Paçoca', 'Bruno']
// let index = array.indexOf('Paçoca') // 0
// array[index] = 'Anninha'

// 12- Como transformar um array em uma string?
// R: assumindo que existe uma variável chamada 'array' que contém um array
// array.toString()

// 13- Como encontrar o índice de um elemento?
// R: assumindo que existe uma variável chamada 'array' que contém um array
// array.indexOf('valor procurado')
